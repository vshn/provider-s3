---
apiVersion: pkg.crossplane.io/v1beta1
kind: DeploymentRuntimeConfig
metadata:
  name: providerconfig-s3
spec:
  deploymentTemplate:
    spec:
      selector: {}
      template:
        spec:
          containers:
            - env:
                - name: LOG_LEVEL
                  value: "2"
              name: package-runtime
